node_modules/
*.log
.DS_Store

# Build artifacts — the Windows exe is committed on purpose (see dist/),
# everything else under dist/ is regenerated by `npm run build:exe`.
dist/*
!dist/Dota2Tracker.exe
